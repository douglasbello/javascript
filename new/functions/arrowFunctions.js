// um jeito de declarar a função
const multiply = (num1, num2) => {
    const result = num1 * num2
    return result
}

// outro jeito
const multiply2 = (num1, num2) => {
    return num1 * num2
}

const multiply3 = (num1, num2) => num1 * num2
